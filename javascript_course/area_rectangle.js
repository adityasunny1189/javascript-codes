const length = 23;
const bredth = 34;

//taking the argument and returning area through arrow function

const area = (len,bre) => (len*bre);

console.log(`Area of the given rectangle is ${area(length,bredth)}`);